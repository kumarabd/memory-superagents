{
  "memory": {
    "command": "/bin/bash",
    "args": [
      "${CLAUDE_PLUGIN_ROOT}/mcp-server/run-mcp-server.sh"
    ],
    "env": {
      "DATABASE_URL": "${DATABASE_URL}",
      "OPENAI_API_KEY": "${OPENAI_API_KEY}"
    }
  }
}
